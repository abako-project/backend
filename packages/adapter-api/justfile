default:
    @just --list

install:
    pnpm install --frozen-lockfile

build:
    pnpm run build

dev:
    PORT=3000 NODE_ENV=development pnpm run start:dev

start:
    PORT=3000 NODE_ENV=production pnpm run start:prod
